version: '3.1'

services:
  mongo:
    image: mongo
    container_name: mongo
    restart: always
    ports:
      - 27017:27017
    volumes:
      - ./data:/data/db
    networks:
      - iteasyopsnetwork

  backend:
    build: 
      context: .
    image: backend
    container_name: backend
    restart: always
    environment:
      - TZ=Asia/Seoul
      - ENVIRONMENT=prod
    depends_on:
      - mongo
    networks:
    - iteasyopsnetwork

networks:
  iteasyopsnetwork:
    external: true
